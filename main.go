package main

func main() {

	StartServer()

	// reader := bufio.NewReader(os.Stdin)
	//
	// fmt.Print("Enter url: ")
	// url, _ := reader.ReadString('\n')
	//
	// key := genUrlKey(url)
	//
	// db := make(map[string]string)
	//
	// db[key] = url
	//
	// fmt.Printf("Key: %q, Url: %q", key, url)
}
